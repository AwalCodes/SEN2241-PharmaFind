[project]
name = "sen2241-pharmafind"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "psycopg2-binary",
]

[tool.vercel]
entrypoint = "backend.main:app"
