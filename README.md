# SEN2241-PharmaFind

Simple final project for SEN2241.

## Run backend (FastAPI)

1. Go to backend folder:
   `cd backend`
2. Install dependencies:
   `pip install -r requirements.txt`
3. Start server:
   `uvicorn main:app --reload`
4. Open Swagger docs:
   `http://127.0.0.1:8000/docs`